# 🖼️ CNN Model Optimization: Lessons from CIFAR-10

본 문서는 CIFAR-10 이미지 분류 프로젝트를 통해 습득한 **딥러닝 모델 최적화 및 규제(Regularization) 전략**에 대해 다룹니다.

---

## 🔍 핵심 문제 정의: Overfitting
- **상황**: 초기 LeNet-5 모델 적용 시, 훈련 데이터에 대한 과도한 적합으로 검증 데이터 정확도가 정체되는 현상 발생.
- **분석**: 데이터 복잡도에 비해 모델의 수용력(Capacity)이 낮거나, 학습을 제어할 장치가 부족함.

## 🛠️ 최적화 전략 (Optimization Strategies)
1. **Batch Normalization 적용**
   - 각 층의 입력 분포를 정규화하여 학습 속도를 개선하고, 내부 공변량 변화(Internal Covariate Shift) 문제를 완화함.
2. **Dropout & L2 Regularization**
   - **Dropout (0.5)**: 뉴런의 연결을 무작위로 생략하여 특정 뉴런에 대한 의존도를 낮춤.
   - **L2 정규화**: 가중치 감쇠(Weight Decay)를 통해 가중치 값이 비대해지는 것을 방지하여 일반화 성능 향상.
3. **Hyperparameter Tuning**
   - Adam Optimizer를 채택하고 Learning Rate Scheduler를 활용하여 전역 최적점(Global Minimum)에 안정적으로 수렴하도록 유도.

## 📈 성과 및 인사이트
- **결과**: 최종 테스트 데이터셋 기준 **정확도($\text{Accuracy}$) 81.61%** 달성.
- **인사이트**: 모델의 깊이(Depth)를 늘리는 것보다, 적절한 규제 기법을 통해 **편향-분산 상충관계(Bias-Variance Trade-off)**를 조절하는 것이 실질적인 성능 향상의 핵심임을 체득함.

---
> 🔗 **관련 프로젝트**: [CIFAR-10 Image Classification](https://github.com/hwan1111/study-archive/tree/main/Projects/CIFAR10-Classification)